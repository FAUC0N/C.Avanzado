#include <stdio.h>

struct Data{
    char* key;
    char* message;
};

struct Node{
    struct Data* data;
    struct Node* Key;
};

struct Node* createNode(char* key, char* value){
    struct Node* newNode;
    
}

void main(){

}
